# Copy this file to ".env" and fill in real values. The .env file is gitignored.

# Django secret key (generate a fresh one for production).
DJANGO_SECRET_KEY=

# Google OAuth credentials from the Google Cloud Console
# (APIs & Services -> Credentials -> OAuth 2.0 Client ID, type "Web application").
GOOGLE_CLIENT_ID=
GOOGLE_CLIENT_SECRET=
